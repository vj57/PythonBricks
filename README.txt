This will have a list of bricks created and a description (when - how) of each brick:
1.) TextToDict.py :
	Class RouterIF and RouterIfRegex does the same job but 1 uses splitline and read line by line whereas regex one uses regex groups. Efficiency of both is yet to be measured. 
